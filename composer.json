{
  "name": "dknauss/keel",
  "description": "Sane, individually-toggleable WordPress defaults — security, updates, privacy, UX, and performance.",
  "type": "wordpress-plugin",
  "license": "GPL-3.0-or-later",
  "require": {
    "php": ">=7.4"
  },
  "require-dev": {
    "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
    "wp-coding-standards/wpcs": "^3.2"
  },
  "scripts": {
    "lint": "phpcs --standard=phpcs.xml",
    "lint:fix": "phpcbf --standard=phpcs.xml",
    "test": "php tests/unfiltered-html.php && php tests/reserved-usernames.php && php tests/force-classic-editor.php && php tests/admin-ux-media.php && php tests/helper-columns.php && php tests/post-passwords.php && php tests/mail-failure.php && php tests/environment-indicator.php && php tests/headers.php && php tests/password-scoping.php && php tests/site-health.php"
  },
  "config": {
    "allow-plugins": {
      "dealerdirect/phpcodesniffer-composer-installer": true
    }
  }
}
