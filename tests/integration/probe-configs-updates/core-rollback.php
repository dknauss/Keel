<?php
/**
 * Core Rollback — nothing to configure.
 *
 * Present rather than absent so the absence is a statement. This plugin has no
 * settings: it adds one screen that lists core releases and reinstalls the one
 * you pick. It takes no position on automatic updates, and the policy rows in
 * the matrix read as stock for that reason and not because anything was left
 * unconfigured.
 *
 * @package Keel
 */

echo 'nothing to configure';
